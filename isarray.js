const friends = ['John', 'Peter', 'Bob', 'Anna', 'Oliver'];

const num = [];
const food = 'rosegolla';
const age = 25; // Fix: age variable is now declared

console.log(Array.isArray(friends)); // true
console.log(Array.isArray(num)); // true
console.log(Array.isArray(food)); // false
console.log(Array.isArray(age)); // false
